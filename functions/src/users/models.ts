export type User = {
    username: string;
    usernameCounter: number;
  };

export type Adjective = {
    count: number;
    random: number;
  };

export type Noun = {
    count: number;
    random: number;
  };

export type Verb = {
    count: number;
    random: number;
  };
