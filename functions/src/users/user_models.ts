export type User = {
    username: string
    usernameCounter: number
    createdAt?: Date
    updatedAt?: Date
  }

export type Adjective = {
    count: number;
    random: number;
  };

export type Noun = {
    count: number;
    random: number;
  };

export type Verb = {
    count: number;
    random: number;
  };
